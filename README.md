# terraform-kong-cassandra

Small repo for playing around using terraform to setup docker environment for kong backed by cassandra.

## Terraform

Check everything is oke.

`terraform plan`

Apply changes.

`terraform apply`